[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
dbt-core = "*"
dbt-databricks = "*"
dbt-adapters = "1.15.3"
click = ">=8.0.0"
rich = ">=13.0.0"
pydantic = ">=2.0.0"
pyyaml = ">=6.0"
jinja2 = ">=3.0.0"
databricks-cli = ">=0.17.0"
openai = ">=1.0.0"
anthropic = ">=0.18.0"
requests = ">=2.25.0"
google-generativeai = "*"

[dev-packages]
dbt-autofix = "*"
pytest = ">=7.4.0"
pytest-click = ">=1.1.0"
pytest-mock = ">=3.11.0"
pytest-cov = ">=4.1.0"
pytest-xdist = ">=3.3.0"
pyfakefs = ">=5.2.0"
freezegun = ">=1.2.0"

[requires]
python_version = "3.13"
